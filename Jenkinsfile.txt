pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('C:/Jenkins/hehe/test.bat') {
                    /* execute commands in the scripts directory */
                }
            bat 'C:/Jenkins/hehe/test.bat'
            }
        }
        stage('Test') {
            steps {
                dir('C:/Jenkins/hehe/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Jenkins/hehe/test.bat'
            }
        }
      stage('Package') {
            steps {
                dir('C:/Jenkins/hehe/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Jenkins/hehe/test.bat'
            }
        }
        stage('Deploy') {
            steps {
                dir('C:/Jenkins/hehe/test.bat') {
                    /* execute commands in the scripts directory */
                }
                bat 'C:/Jenkins/hehe/test.bat'
            }
        }
    }
}
